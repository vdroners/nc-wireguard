# NC WireGuard port plan (checked-in summary)

Port wg-dashboard monitoring UI into Nextcloud app `nc_wireguard` with admin-only PHP proxy, Vue SPA, sidecar hardening, and post-gate infra cutover.

See implementation in repo root; verification via `make gate-local`.
